<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPOI"
        tools:targetApi="31">
        <activity
            android:name="com.mypoi.location.AddLocationActivity"
            android:exported="false"
            android:label="Aggiungi Posizione"
            android:theme="@style/Theme.AppCompat"/>
        <activity
            android:name="com.mypoi.category.AddCategoryActivity"
            android:exported="false"
            android:label="@string/title_activity_add_category"
            android:theme="@style/Theme.MyPOI" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBCRajqwumwLUPtB3mMqTtZP--hrpGWZv4" />

        <activity
            android:name=".MapsActivity"
            android:exported="true"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>